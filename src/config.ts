import { StorageConfig } from '@ionic/storage';

export const kodiConfig = {
  storage: {
    name: 'wako',
    version: 1
  } as StorageConfig,
  storageCache: {
    name: 'wako_cache',
    version: 1
  } as StorageConfig
};
